// Where user can tag and save translation
import React from 'react';

export default class TranslationSave extends React.Component {
  render() {
    return (
        <div>
            Save
        </div>
    );
  }
}
